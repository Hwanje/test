# 프로젝트: 키오스크 연습장 (해커톤, 마감 7/19 09:00)

## 무엇을 만드는가
고령층이 병원·카페 무인 키오스크 앞에서 시간 압박과 뒷사람 눈치 때문에
포기하고 돌아서는 문제를 해결한다.
"가르치는 앱"이 아니라 "실패해도 되는 연습 공간"을 만든다.

## 절대 착각하면 안 되는 것 ★★★
시뮬레이터 안쪽의 '가짜 키오스크 화면'은 실제 키오스크처럼 불편하고
복잡해야 한다. 단계를 줄이거나 친절하게 개선하면 연습의 의미가 사라진다.
쉽고 크고 친절해야 하는 것은 그 바깥의 도우미 UI(안내 문구, 음성, 힌트,
결과 카드)다.
→ 안쪽 = 현실 재현 / 바깥 = 배려. 이 둘을 절대 섞지 마라.

## 절대 규칙
1. 제품 코드는 index.html 단 하나. CSS·JS 전부 인라인.
2. npm·빌드도구·번들러·프레임워크·CDN·외부 라이브러리 전면 금지.
3. 우리 코드에서 네트워크 요청 0건. 서버·DB·로그인·회원가입 없음.
4. 상태는 전부 메모리(JS 변수). localStorage도 쓰지 않는다.
5. 파일을 새로 만들거나 구조를 바꾸기 전에 반드시 먼저 물어본다.
6. 지시하지 않은 기능을 임의로 추가하지 않는다.

## 디자인 시스템 (수치 고정, 임의 변경 금지)
- 폰트: system-ui, 'Apple SD Gothic Neo', 'Malgun Gothic', sans-serif
- 크기: 본문 22px / 키오스크 버튼 28px / 도우미 안내 26px / 제목 38px
- 버튼 최소 높이 80px, 버튼 사이 간격 16px
- 색: 배경 #FFFFFF · 텍스트 #1A1A1A · 강조 #1F6FEB · 성공 #0F7B3E · 주의 #C2410C
- 명도 대비 4.5:1 이상. 회색 배경 위 연회색 텍스트 금지.
- 모서리 16px. 애니메이션은 '힌트 반짝임' 하나만 허용.

## 문구 규칙
- 사용자에게 보이는 모든 문구는 파일 상단 const TEXT = {...} 에 모은다.
- 사용자를 탓하지 않는다. "잘못 눌렀습니다" ✕ → "괜찮아요, 다시 해볼까요?" ○
- 외래어 금지. 불가피하면 병기: "결제(돈 내기)"
- 존댓말, 한 문장 20자 이내 지향.

## 작업 방식
- 한 번에 마일스톤 하나만. 끝나면 멈추고 3줄 보고:
  (1) 한 것 (2) 시연 중 깨질 수 있는 지점 (3) 다음 할 일
- 코드 수정 후 검증 시나리오를 스스로 적고 점검한 뒤 보고한다.
- 주석은 한국어, 함수명은 영어.
- 남은 시간 대비 무리한 작업이면 시작하기 전에 먼저 말린다.

## 기록 의무 (대회 심사 항목)
DECISIONS.md 를 유지한다. 설계 결정마다 3줄 추가:
무엇을 정했나 / 왜 / 버린 대안은 무엇인가.
이 파일은 결과보고서의 근거 자료로 제출된다.
