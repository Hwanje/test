# 설계 결정 기록

## M1-1. 화면을 데이터로 그리는 범용 렌더러
- 무엇을 정했나: 시나리오를 SCENARIOS 객체로 두고, layout 값(grid/list/keypad/confirm/done)에 따라 renderStep()이 화면을 자동으로 그린다.
- 왜: 카페 주문 등 새 시나리오를 데이터 추가만으로 넣기 위해.
- 버린 대안: 단계마다 HTML을 직접 만드는 방식 — 단계 추가마다 코드 수정이 필요해 버림.

## M1-2. 숫자판은 여섯 자리면 통과
- 무엇을 정했나: 생년월일 입력은 정확한 값(480312) 검사 없이 여섯 자리를 채우면 정답 처리한다.
- 왜: 연습의 목적은 "숫자판을 눌러 입력을 끝내는 경험"이지 특정 값 암기가 아니라서.
- 버린 대안: 480312와 정확히 일치해야 통과 — 어르신이 자기 생년월일을 눌렀을 때 틀렸다고 하면 탓하는 느낌이라 버림.

## M1-3. [다시 듣기]는 지금은 문구 다시 보여주기
- 무엇을 정했나: M1에서는 음성이 없으므로 [다시 듣기]를 누르면 안내 문구를 다시 보여준다(틀림 힌트 → 원래 안내로 복귀).
- 왜: 버튼 자리는 M1에서 잡아두고, 음성은 이후 마일스톤에서 연결하기 위해.
- 버린 대안: 음성 전까지 버튼 숨기기 — 화면 배치가 나중에 바뀌면 어르신이 혼란스러워서 버림.

## M2-1. 숫자판 단계의 힌트 목표물
- 무엇을 정했나: 숫자판 단계에서는 여섯 자리를 다 채우기 전엔 숫자판 전체를, 다 채우면 [확인] 버튼을 반짝인다.
- 왜: 아직 입력 중일 때 특정 숫자 하나를 반짝이면 "그 숫자만 누르라"는 오해를 주기 때문.
- 버린 대안: 항상 [확인]만 반짝이기 — 입력 전에 [확인]을 누르게 유도해 오답을 만들어서 버림.

## M2-2. 힌트 시계는 조작할 때마다 다시 센다
- 무엇을 정했나: 6초/12초 힌트 시계는 단계 시작과 모든 조작(오답, 숫자 입력, 다시 듣기)마다 처음부터 다시 센다.
- 왜: 스스로 움직이고 있는 동안 힌트가 끼어들면 재촉으로 느껴지기 때문. 힌트는 '멈춰 있을 때'만 나온다.
- 버린 대안: 단계 시작 시각 기준 고정 12초 — 천천히 누르는 중에도 힌트가 떠서 버림.

## M2-3. "여기예요"는 정답 버튼 안 모서리에 붙인다
- 무엇을 정했나: "여기예요" 표시는 정답 버튼 내부 오른쪽 위에 작은 말풍선처럼 붙인다.
- 왜: 격자 배치에서 버튼 밖에 형제 요소를 끼우면 칸이 밀려 화면이 흐트러지기 때문.
- 버린 대안: 버튼 옆에 별도 칸 추가 — grid 열이 밀려 실제 키오스크 배치가 깨져서 버림.

## M3-1. tip 하나로 화면 조언과 인쇄 순서표를 겸한다
- 무엇을 정했나: 각 단계의 tip 필드를 결과 카드의 "다음엔 이렇게 하세요"와 인쇄 순서표의 번호 목록에 함께 쓴다.
- 왜: 화면에서 본 조언과 지갑 속 종이의 문구가 같아야 부모님이 헷갈리지 않는다.
- 버린 대안: 인쇄용 문구를 따로 두기 — 데이터가 두 벌이 되어 어긋날 위험이 있어 버림.

## M3-2. 1분 미만이면 "총 O초 만에 끝냈어요"로 줄인다
- 무엇을 정했나: 총 시간이 1분 미만이면 "총 0분 45초"가 아니라 "총 45초 만에 끝냈어요"로 보여준다.
- 왜: "0분"은 어르신에게 어색하고 읽는 데 방해가 된다.
- 버린 대안: 항상 "O분 O초" 고정 — 짧은 기록에서 어색해서 버림.

## M3-3. 인쇄는 순서표만 남기고 전부 숨기는 방식
- 무엇을 정했나: @media print에서 body 바로 아래 print-sheet 외 모든 요소를 숨기고, 순서표는 완료 시점 기록으로 새로 그린다.
- 왜: 화면용 요소를 하나씩 숨기면 빠뜨리기 쉽고, A4 한 장 보장이 어렵다.
- 버린 대안: 결과 카드 화면을 그대로 인쇄 — 버튼·막대가 섞여 지갑용 한 장이 안 되어 버림.

## M4-1. 카페 시나리오는 데이터만 추가 (렌더러 무수정)
- 무엇을 정했나: 카페 8단계를 기존 layout 5종만으로 표현하고, '카드 넣기' 단계는 done 배치(안내문+확인)를 재사용했다.
- 왜: M1 렌더러 설계 검증이 목표였고, 실제로 코드 수정 0줄로 새 시나리오가 돌아갔다.
- 버린 대안: '카드 넣기' 전용 layout 신설 — 안내문+단추 하나라는 점에서 done과 동일해 불필요했다.

## M4-2. 모드 선택은 홈의 두 단추로, 기본은 연습하기
- 무엇을 정했나: 홈 상단에 [연습하기]/[혼자 해보기] 두 단추를 두고, 고른 상태로 시나리오 카드를 누르면 그 방식으로 시작한다.
- 왜: 화면(장면)을 늘리지 않고 홈에서 한눈에 고르게 하기 위해. 처음 온 사람은 연습부터가 자연스러워 기본값은 연습하기.
- 버린 대안: 시나리오 선택 후 모드 선택 화면 추가 — 누르는 횟수가 늘고 M1의 3화면 구조가 깨져서 버림.

## M4-3. 혼자 해보기에서 오답은 '무반응'으로 둔다
- 무엇을 정했나: 혼자 해보기에서 틀린 버튼을 누르면 화면만 그대로 있고 어떤 표시·소리도 내지 않는다. [처음으로]와 진행 표시는 남긴다.
- 왜: 실제 키오스크도 틀린 곳을 누르면 아무 일도 안 일어난다(현실 재현). 부정 표현 금지 원칙에도 맞는다.
- 버린 대안: 은은한 알림 표시 — '틀렸다'는 신호 자체가 압박이 되어 버림.

## M5-1. 체험 모드는 연습 화면을 재사용하되 'demo' 상태로 완전 격리
- 무엇을 정했나: 체험 모드는 별도 화면을 만들지 않고 연습 화면에 demo 표시를 붙여 카운트다운·대기줄만 보이게 바꾼다. 시계는 시작·이탈·종료 시 전부 끈다.
- 왜: 렌더러를 그대로 써서 코드 중복이 없고, 시간 표시는 demo 상태에서만 존재하므로 본 연습에 새어 나갈 길 자체가 없다.
- 버린 대안: 체험 전용 화면 복제 — 키오스크 렌더러가 두 벌이 되어 마감 전 수정이 두 배로 들어 버림.

## M5-2. 어려운 말 밑줄은 정답 버튼 안에는 긋지 않는다
- 무엇을 정했나: 사전 단어 밑줄을 제목·안내문·오답 버튼에만 긋고, 정답 버튼 글자에는 긋지 않는다. 단어를 눌러도 버튼 동작은 그대로 이어진다.
- 왜: 정답 글자에 그으면 정답을 누를 때마다 말풍선이 떠 주 진행이 방해받는다. 헷갈리는 말(수납 등)은 대부분 오답 버튼이라 학습 효과는 그대로다.
- 버린 대안: 단어 클릭 시 버튼 동작 차단 — 정답 글자를 눌러도 안 넘어가 "고장"으로 느껴져 버림.

## M5-3. 글씨 크기의 기준은 CSS 변수 --scale 하나
- 무엇을 정했나: 도우미 UI 글자만 calc(기준값 × --scale)로 통일하고, 키오스크·체험·인쇄 영역은 고정 px를 유지한다. 기본은 1.0([가-]).
- 왜: 변수 하나만 바꾸면 전체 도우미 UI가 함께 커져 빠뜨리는 곳이 없고, 현실 재현 영역은 구조적으로 영향을 받지 않는다.
- 버린 대안: body 전체 배율 적용 — 키오스크 안 글자까지 커져 '안쪽=현실 재현' 원칙이 깨져서 버림.

## M5-4. 방해 요소는 시나리오 데이터(ads/terms)로 넣는다
- 무엇을 정했나: 광고 문구와 약관 문구를 SCENARIOS의 ads/terms 필드에 두고, 키오스크 틀(광고 띠·이전/다음·약관)은 렌더러가 공통으로 그린다. 결제 선택지 '휴대폰 결제'는 실제 표기인 '간편결제'로 바꿨다.
- 왜: 병원·카페가 서로 다른 광고/약관을 갖는 게 현실적이고, 새 시나리오도 데이터만으로 방해 요소까지 갖춘다. '간편결제'는 사전 12단어에 있어 밑줄 학습과도 연결된다.
- 버린 대안: 광고·약관을 전역 고정 문구로 — 병원 키오스크에 카페 광고가 떠서 신뢰도가 깨져 버림.

## M5-5. 오터치 방지는 문서 캡처 단계에서 한 번만 검사한다
- 무엇을 정했나: 같은 버튼 400ms 중복 무시를 각 버튼이 아니라 문서 전체의 캡처 단계 클릭 검사 하나로 처리하고, 진동·눌림 피드백도 전 버튼 공통 규칙으로 걸었다.
- 왜: 버튼마다 검사 코드를 넣으면 새 버튼에서 빠뜨린다. 한 곳이면 이후 추가되는 버튼도 자동으로 보호된다.
- 버린 대안: 버튼별 개별 처리 — 이미 버튼이 30개가 넘어 누락 위험이 커서 버림.

## M5-6. 복사 문안은 인쇄 순서표와 같은 데이터에서 만든다
- 무엇을 정했나: [순서표 글로 복사하기]는 인쇄 순서표와 동일하게 시나리오 tip과 어려웠던 단계(★)로 문안을 만들고, 복사 실패·미지원이면 글상자를 띄워 전체 선택해 준다.
- 왜: 종이(인쇄)와 문자(복사)의 내용이 다르면 가족과 어르신이 서로 다른 순서를 보게 된다. 폴백은 어떤 기기에서도 기능이 죽지 않게 한다.
- 버린 대안: 복사 실패 시 오류 문구만 표시 — 어르신 입장에선 막다른 길이라 버림.

## M6-1. 제출 전 안정화: 말풍선 위치와 좁은 화면 여백만 손봄
- 무엇을 정했나: 기능 동결 상태에서 점검으로 찾은 두 가지만 고쳤다 — 말풍선이 [처음으로]를 가리던 것(도우미 버튼 위로 이동 + 바깥 누르면 닫힘), 360px에서 우측 상단 단추가 홈 제목과 겹치던 것(좁은 화면 여백 보정).
- 왜: 시연 중 "나가는 길이 막히는" 상황과 "겹쳐 보이는 첫 화면"은 치명적이라 제출 전 반드시 제거해야 했다.
- 버린 대안: 말풍선을 가운데 창으로 변경 — 연습 화면을 가려 '연습이 중단되지 않는다' 원칙이 깨져 버림.

## M7-1. 정답을 '화면'이 아니라 '오늘의 볼일'에서 꺼낸다
- 무엇을 정했나: step의 correct 표시를 없애고, 볼일 무관 단계는 fixedAnswer, 볼일 단계는 answerKey로 미션 answers에서 정답을 조회한다. 자리표시자({answer}{dept} 등)로 안내·확인 문안도 볼일을 따라간다.
- 왜: 고정 정답은 '이 화면의 이 순서' 암기가 되어 처음 보는 키오스크에 전이되지 않는다. 판단의 근거를 화면 밖(볼일)에 둬야 배우는 게 '판단하는 법'이 된다.
- 버린 대안: 미션별 시나리오 복제 — 데이터가 미션 수만큼 늘어나 관리 불가라 버림.

## M7-2. 2차 힌트(범위 축소)는 섞인 배치를 보고 즉석 계산한다
- 무엇을 정했나: grid/list의 "위에서 O번째 줄" 힌트는 데이터에 적지 않고, 힌트를 보여주는 순간 실제 정답 버튼의 줄을 세어 만든다. 위치 개념이 없는 단계(숫자판·확인)만 hint2를 데이터로 둔다.
- 왜: 매 판 버튼을 섞기 때문에 데이터에 적은 위치는 반드시 틀리게 된다.
- 버린 대안: 셔플 결과를 고정 시드로 묶기 — '매번 다른 배치'라는 목적 자체가 사라져 버림.

## M7-3. 회차 규칙: 연습 1회차만 안내, 혼자는 늘 볼일
- 무엇을 정했나: 시나리오별 연습 회차를 세서 1회차는 볼일 없는 안내 모드(성공 경험 보장), 2회차부터 볼일+섞기+간판 교대. 혼자 해보기는 회차와 무관하게 항상 볼일 모드. 생년월일 숫자판은 계속 자리수만 검증한다(M1-2 유지).
- 왜: 첫 판부터 판단을 요구하면 포기하고, 계속 안내만 하면 배움이 없다. 난이도가 회차를 따라 자연히 오르게 했다.
- 버린 대안: 홈에 난이도 선택 버튼 — 어르신에게 선택 부담을 주고 홈이 복잡해져 버림.

## M7-4. 간판(skin)은 용어 사전 치환으로 만든다
- 무엇을 정했나: B간판은 별도 데이터가 아니라 {접수→진료접수, 수납→원무수납, 증명서 발급→제증명 발급} 치환표 하나로 만들고, 화면·안내·힌트 문구에 일괄 적용한다. 정답 판정은 치환 전 원래 말로 한다.
- 왜: "말은 달라도 순서는 같다"를 체득시키는 게 목적이므로 구조는 한 벌이어야 하고, 치환표만 늘리면 C·D간판도 즉시 추가된다.
- 버린 대안: 간판별 시나리오 사본 — 수정이 두 배로 들고 '구조가 같다'는 보장이 깨져 버림.

## M8-1. 긴 숫자는 음성으로만 한 자리씩 끊어 읽는다
- 무엇을 정했나: 읽어주기 직전에 다섯 자리 이상 이어진 숫자만 "4, 8, 0, 3, 1, 2"처럼 끊는다. 화면 표시는 480312 그대로 두고, 대기번호 23번·4,500원 같은 짧은 수는 수량으로 읽는다.
- 왜: 음성이 생년월일 480312를 "사십팔만삼백십이"로 읽어 어르신이 알아들을 수 없었다. 생년월일은 수량이 아니라 자릿수 나열이다.
- 버린 대안: 모든 숫자를 한 자리씩 읽기 — "대기번호 이십삼 번"이 "이, 삼 번"이 되어 더 이상해서 버림.
